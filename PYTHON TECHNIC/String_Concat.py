#ex28 input 5 string by for loop and concat_string and print
#สร้าง stirng ว่าง เพื่อ นำ สตริง มาเรียงต่อ
concat_string=''
#input string by loop
for i in range(5):
    str=input("enter string:")
    concat_string =concat_string+str
print(concat_string)
# -> p r a c h = prach